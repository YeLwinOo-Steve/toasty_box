name: toasty_box
description: A beautiful toast library with heart-melting animations. Currently support 5 types of toast - MessageToast, WidgetToast, SuccessToast, WarningToastt, ErrorToast
version: 1.0.2
homepage: https://github.com/YeLwinOo-Steve/toasty_box

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter: